{
  "name": "neon-bar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/bar-alpha.mjs --check && tsc --noEmit && vite build",
    "preview": "vite preview",
    "alpha": "node scripts/bar-alpha.mjs",
    "test": "vitest run",
    "e2e": "playwright test",
    "check": "npm run build && npm run test && npm run e2e",
    "shots": "node scripts/shots.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "happy-dom": "^15.11.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
